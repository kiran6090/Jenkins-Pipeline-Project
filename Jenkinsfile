@Library('Jenkins-Shared-Library') _

pipeline {
    agent any
    stages {
        stage('Test Library') {
            steps {
                echo 'Library loaded successfully!'
            }
        }
    }
}

